services:
  speedtest:
    build:
      context: .
      dockerfile: docker/Dockerfile.frankenphp
    container_name: libre-speedtest-frankenphp
    ports:
      - "${WEBPORT:-8080}:8080"
    restart: always
